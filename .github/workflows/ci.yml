name: CI

on:
  pull_request:
    branches: ["dev", "main"]
  push:
    branches: ["dev", "main"]

permissions:
  id-token: write
  contents: read

jobs:
  format:
    uses: ./.github/workflows/format.yml

  test:
    needs: format
    uses: ./.github/workflows/test.yml