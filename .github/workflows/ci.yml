name: CI

on:
  pull_request:
    branches: ["dev", "main"]
  push:
    branches: ["dev", "main"]

permissions:
  id-token: write
  contents: read

jobs:
  # 🧪 빌드 및 테스트 (Test)
  test:
    uses: ./.github/workflows/test.yml
