name: Format

on:
  workflow_call:

jobs:
  format:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v6
      
      - uses: jdx/mise-action@v4

      - name: Swift format
        run: |
          swiftformat .
          git diff --stat
          git diff
          git diff --exit-code